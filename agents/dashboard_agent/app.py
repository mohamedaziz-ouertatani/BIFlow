"""Streamlit/Dash entrypoint for the BIFlow dashboard.

TODO (owner): implement the dashboard UI. For an MVP with Streamlit, this
file should be runnable via `streamlit run agents/dashboard_agent/app.py`
and render the layout produced by layout_builder.build_layout().
"""


def main() -> None:
    """Renders the dashboard application.

    TODO (owner): implement — read a DashboardSpec/layout (e.g. from a
    shared store or passed via config) and render it with Streamlit or Dash.
    """
    raise NotImplementedError("TODO: implement dashboard app entrypoint")


if __name__ == "__main__":
    main()
