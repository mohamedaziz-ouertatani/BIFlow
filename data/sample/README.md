# Sample data

TODO (owner): add a small sample dataset here so the end-to-end test
(`tests/test_end_to_end.py`) and each agent's own tests can run against
something real before full datasets are ready. Keep it small enough to
commit to git (a few hundred rows is plenty).
